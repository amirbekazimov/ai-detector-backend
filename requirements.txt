# Render-compatible requirements - NO RUST DEPENDENCIES
# Use Python 3.11 compatible versions (STABLE)
fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
# Use stable pydantic version for Python 3.11
pydantic==2.7.0
python-dotenv==1.0.0
# Replace python-jose with PyJWT (no Rust)
PyJWT==2.8.0
# Replace passlib with argon2 (no Rust)
argon2-cffi==23.1.0
# Add email validator for pydantic email fields
email-validator==2.0.0
python-multipart==0.0.6
# Additional production dependencies
gunicorn==21.2.0
