# Render-compatible requirements - NO RUST DEPENDENCIES
# Use older FastAPI version compatible with pydantic v1
fastapi==0.95.2
uvicorn[standard]==0.22.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
# Use older pydantic version without Rust dependencies
pydantic==1.10.13
python-dotenv==1.0.0
# Replace python-jose with PyJWT (no Rust)
PyJWT==2.8.0
# Replace passlib with argon2 (no Rust)
argon2-cffi==23.1.0
python-multipart==0.0.6
# Additional production dependencies
gunicorn==21.2.0
# Ensure no Rust dependencies
--only-binary=all
--prefer-binary
